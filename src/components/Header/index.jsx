import React from 'react';

import { HeaderWrapper, AppTitle } from './styled';

const Header = () => {
  return (
    <HeaderWrapper>
      <AppTitle>To-do List App</AppTitle>
    </HeaderWrapper>
  );
};

export default Header;
